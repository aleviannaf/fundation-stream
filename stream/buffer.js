const buf = Buffer.from("Alexandre")
//É uma forma que o node encontrou de trabalha de forma nativa com dados binários
// O buff é uma forma de trabalhar com binaria em um hexadecimal ou etc

console.log(buf)
console.log(buf.toJSON())